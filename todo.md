# Remaining Work

- `foot_on_line_minimizes_distance` earns its name through the theorem that `foot_on_line p l` minimizes `dist2 p y` over all `y` with `on_line y l`, derived from the `o5land` parametrization and `o5_min_dist`.
- `fold_O7` becomes case-complete by branching on `on_line p1 l1` to emit `perp_through p1 l2` for a point already incident to its target line, yielding the satisfaction theorem for every solvable configuration together with the impossibility lemma that no well-formed crease meets the O7 constraint when `l1` is parallel to `l2` and `p1` lies off `l1`, totalizing `O7_unified`.
- The constructor `CF_O6` realizes the axiom O6 itself by carrying the general-position crease of `O6_general_constructible` — the O2 fold of `p1` and its reflection via `reflect_via_perp_bisector` — in place of the midpoint approximation `fold_O6_approx`, with the enumeration soundness, completeness, and `GoodFold` proofs transported to the new payload.
- The trisection configuration `trisection_setup` receives its satisfaction certificate — `satisfies_O6_constraint` for the trisecting fold, derived from the depressed triple-angle cubic through `beloch_fold_satisfies_O6` — so the worked example carries the same guarantee as the heptagon.
- The three period towers `Tower`, `Tower2`, and `Tower3` collapse into one tower parameterized by a closure-witness record — field operations with quadratic, cubic, quintic, and septic root constructors — whose single parametric power-sum lemma subsumes `sub_sq_ON` and `par_prod_pow_ON2`/`par_prod_pow_ON3`, making each fold level and every future rung one instantiation.
- The two-fold layer is grounded at the axiom level by a formalized simultaneous two-fold axiom system — paired creases under mutual alignment constraints — with the realizability theorem that the crease `{t, -1, -t^4}` arises from such a manipulation, the two-fold analogue of `beloch_fold_satisfies_O6` that fully earns the README's claim of a genuine two-fold construction.
- The necessity direction of the three-fold n-gon characterization — that `OrigamiNum3 (cos (2 * PI / INR n))` implies `is_7_smooth (euler_phi n)`, which closes `ngon_three_fold_iff` as the exact analogue of `ngon_two_fold_iff` — requires an `OrigamiNum3` field-degree bound obtained from a degree-7 simple field extension `Sept7F` and a 7-smooth septic tower `o7tower`, discharged through the generic `tower_law_div` and `divisor_7_smooth`, mirroring the `o5tower`/`Quint5F` degree theory that grounds `OrigamiNum2_field_degree_smooth`.
